### Blogdown R Script Help Guide ----
# Load library
library(blogdown)

# View website (from this project)
blogdown::serve_site()

# Stop server (if need be)
# blogdown::stop_server()

# Build site again (if need be)
# blogdown::build_site()
